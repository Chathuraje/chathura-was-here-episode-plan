# Google Maps JavaScript API key, used only by the Locations map.
# Copy this file to .env.local and put your own key in. Without it the
# Locations page still lists every place and links out to Google Maps.
# Restrict the key to the Maps JavaScript API and to your own referrers.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
